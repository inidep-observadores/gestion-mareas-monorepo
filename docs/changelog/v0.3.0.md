# Changelog v0.3.0

### [v0.3.0] - UNRELEASED

## 2026-03-30 - Implementación del Flujo de Protocolización de Mareas (Fase 1)

### Backend
- **Esquema de Datos**: Incorporación de campos de protocolización (`nro_protocolizacion`, `anio_protocolizacion`, `fecha_protocolizacion`, `fecha_envio_protocolizacion`) al modelo de Marea.
- **Acciones de Flujo**: 
  - Implementación de `FINALIZAR_PROTOCOLIZACION` en `executeAction` con validaciones y persistencia atómica.
  - Sincronización de tipos numéricos (`Int`) para el número de protocolización según requerimiento institucional.
- **API de Protocolización**:
  - Nuevo endpoint `POST /mareas/protocolizacion/confirmar/:id` para la confirmación explícita de datos.
  - Implementación de `enviarAProtocolizacion` preparado para gestión de archivos Word (adjuntos .docx).
- **Base de Datos**: Aplicación de migración manual no destructiva para añadir columnas de protocolización.

### Frontend
- **Vista de Gestión de Protocolización**:
  - Creación de `ProtocolizacionView.vue` con sistema de pestañas para organizar el flujo de trabajo.
  - Implementación de la pestaña "Esperando Confirmación" con listado filtrado de mareas en estado `ESPERANDO_PROTOCOLIZACION`.
- **Diálogo de Confirmación**:
  - Implementación de `ModalConfirmarProtocolizacion.vue` con validaciones de campos (Número, Año, Fecha).
  - Uso de `input type="number"` y `v-model.number` para garantizar integridad de tipos desde la UI.
- **Servicios**: Extensión de `mareas.service.ts` para soportar las nuevas operaciones de protocolización.


## 2026-03-28 - Refactorización del Dashboard de Personal y Categoría "Designados"

### Backend
- **Estado de Observadores**: 
  - Actualización de `getObserverStatus` para priorizar el estado `DESIGNADO` sobre la disponibilidad general cuando existe una marea en estado `DESIGNADA`.
  - Refactorización de `getWorkforceStatus` para poblar la nueva lista `listDesignados` y excluir a estos observadores de las categorías de "Disponibles" y "Descanso", eliminando datos duplicados entre listas.
  - Inclusión del conteo de designados en el objeto de respuesta de la API de workforce status.

### Frontend
- **WorkforceOverview**:
  - **Nueva Categoría "Designados"**: Implementación de una cuarta tarjeta funcional en el panel de Estado del Personal para visualizar observadores con mareas asignadas.
  - **Lógica de Listado Reactiva**: Actualización de la propiedad computada `currentList` para soportar la visualización de la lista de designados con columnas específicas (`Zarpada Estimada`).
  - **Ordenamiento Jerárquico Institucional**: 
    - Implementación de un motor de ordenamiento unificado: **1º Designados, 2º Titulares, 3º Mujeres, 4º Eventuales**.
    - Dentro de cada bloque, se mantiene la precedencia de hombres sobre mujeres para cumplir con el requerimiento de "luego las mujeres".
    - Los eventuales se desplazan sistemáticamente al final de cada categoría operativa.
  - **Visualización de Dona**:
    - Sincronización de los gráficos circulares para que reflejen la composición exacta de sus respectivas listas.
    - **Mejora en Navegando**: Inclusión de un sector específico **Celeste (`#38bdf8`)** para identificar visualmente a los observadores que están en el mar pero ya han sido designados para su próxima misión.
  - **Corrección de Datos de Pesquería**:
    - **Visualización en Modal**: Incorporación del dato de pesquería en la columna central del modal expandido para todas las categorías operativas.
    - **Trazabilidad de Datos**: Limpieza de prefijos de depuración (`[B]`, `[F]`) y estandarización del fallback a "Sin Pesquería" cuando el recurso no está asignado.
    - **Consistencia Marea/Etapa**: Optimización de la lógica para obtener la pesquería desde la marea base cuando aún no existen etapas (estado DESIGNADA).
  - **Pruebas y Calidad**:
    - **Nuevos Tests Unitarios**: Implementación de suite de pruebas para `getWorkforceStatus` validando el mapeo de pesquerías, fallbacks y priorización de etapas.
    - **Limpieza de Logs**: Eliminación de registros de consola de depuración en el servicio de mareas.

## 2026-03-20 - Gestión Especializada de Protocolización y Mejoras de UX

### Backend
- **Acción de Protocolización**:
  - Actualización del método `executeAction` en `MareasService` para procesar y persistir datos específicos de protocolización (`nroProtocolizacion`, `anioProtocolizacion`, `fechaProtocolizacion`).
  - Registro automático en el historial de movimientos de la marea con comentarios opcionales del usuario.

### Frontend
- **Nuevo Diálogo de Protocolización**:
  - Implementación de `FinalizarProtocolizacionDialog.vue` con validaciones de campos obligatorios.
  - **Validación de Negocio**: Restricción que impide que la fecha de protocolización sea anterior a la fecha de finalización de la marea.
  - **Auto-foco Inteligente**: Selección automática del campo "Número Protocolo" al abrir el diálogo para optimizar la carga de datos.
- **Integración Global de Interceptación**:
  - Unificación de la lógica de disparo de acciones en todas las vistas operativas (`PanelOperativoView`, `MareaOperativaDetalleView`, `BandejaView`, `FlujoMareasView`).
  - Interceptación explícita de `FINALIZAR_PROTOCOLIZACION` para evitar el uso del diálogo genérico del sistema.
  - Corrección de regresiones en `BandejaView` relacionadas con la acción de recepción de datos.

### Testing
- **Suite de Pruebas**: Implementación de tests unitarios avanzados en Vitest para validar estados de formulario, validaciones de fecha y comportamiento de botones en el nuevo diálogo.

---

## 2026-03-14 - Seguridad de Observaciones por Rol y Mejoras de Perfil

### Frontend
- **Restricción de Observaciones**:
  - Implementación de control de acceso basado en roles para el campo "Observaciones" de observadores.
  - La visualización y edición ahora está limitada exclusivamente a los roles `admin` y `coordinador` en las vistas `WorkforceOverview` y `ObservadorTimelineDialog`.
  - Uso de propiedades computadas reactivas para garantizar la integridad de la UI según los permisos del usuario.
- **Mejoras en Perfil de Usuario**:
  - **Gestión de Avatar**: Se añadió la funcionalidad para eliminar la foto de perfil actual, permitiendo revertir a la imagen predeterminada.
  - **Visualización de Roles**: Incorporación de un bloque "Roles Asignados" con badges estilizados y formateo legible para identificar rápidamente los permisos del usuario.
  - **Optimización de UX**: Notificaciones instantáneas (toasts) al marcar fotos para eliminar o seleccionar nuevos archivos.

---

## 2026-03-14 - Refactorización de Exportación Excel y Mejoras de UX

### Backend
- **Exportación Excel de Dotación**:
  - Implementación de filtrado dinámico mediante el parámetro `filterValue`.
  - Sincronización total con la lógica del Dashboard utilizando `mareasService.getWorkforceStatus()`.
  - Corrección de etiquetas de estado y ordenamiento jerárquico (Titulares > Femeninos > Eventuales).
  - Mejora estética con colores por condición (Designados, Impedidos, Eventuales) y columna de "Condición" explícita.

### Frontend
- **WorkforceOverview**:
  - Sincronización del botón de exportación con los filtros de vista (ej. ocultar técnicos si el filtro está inactivo).
  - Ajuste de diseño para evitar la superposición del botón de exportación con el badge de total.
  - Mejora en los mensajes de confirmación (toasts) indicando el alcance de la exportación.
- **Stats Service**:
  - Actualización de `downloadWorkforceExport` para propagar filtros al backend sin alterar el contrato de la API.

---

## 2026-03-13 - Gestión Avanzada de Observadores y Dashboard de Personal

### Backend
- **Identidad de Observadores**: 
  - Incorporación de campos `sexo` (Enum: Masculino/Femenino) y `eventual` al modelo de Observador.
  - Actualización de DTOs y lógica de validación para soportar la caracterización del personal.
- **Detección de Designaciones Activas**:
  - Reingeniería del motor de `getWorkforceStatus` para capturar mareas en estado `DESIGNADA` directamente desde el modelo de Marea.
  - Resolución de bug que omitía designaciones sin etapas de marea creadas.
  - Sincronización de reglas de negocio para asegurar que un observador designado sea identificado correctamente en todos los KPIs operativos.

### Frontend
- **Formularios de Administración**: Integración de selectores de sexo y marcas de eventualidad en el CRUD de observadores.
- **Dashboard de Fuerza de Trabajo (WorkforceOverview)**:
  - **Gráfico de Dona Interactivo**: Nueva visualización analítica de la composición de observadores disponibles por categoría (Titulares, Femeninos, Eventuales, Designados).
  - **Filtrado Bidireccional**: Capacidad de segmentar la lista de detalle haciendo clic directamente en los sectores del gráfico.
  - **Identidad Visual Premium**:
    - Código de colores adaptativo: Celeste para **Designados**, Rosa para **Femeninos**, Gris para **Eventuales**.
    - **Badge "Designado"**: Implementación de una etiqueta absoluta en el rincón superior derecho de las filas para identificar mareas activas de manera inmediata.
    - Priorización visual en tabla: las designaciones activas tienen jerarquía sobre otras marcas.
  - **Optimización de UX**:
    - @click.stop preventivo en el gráfico para evitar colapsos accidentales del panel.
    - Sincronización total entre los filtros globales (Observador/Técnico) y el gráfico de dona.
    - **Ordenamiento Jerárquico Unificado**: Implementación de prioridad Titulares > Masculinos > Criterio Dinámico (Días/Nombre) para consistencia en todas las listas operativas.

### Base de Datos
- **Migración v2**: Implementación de `manual_experiencia_observador.sql` para campos de identidad, garantizando consistencia con sistemas legados.

---

## 2026-03-13 - Reversión de Mareas y Gestión de Observadores Inactivos

### Backend
- **Reversibilidad de Estados**:
  - Implementación de la acción `DESHACER_INICIO` para retroceder mareas de `EN_EJECUCION` a `DESIGNADA`.
  - Nueva lógica de limpieza automática de etapas y restauración de fechas de inicio.
  - Soporte para transiciones de cancelación y reasignación desde el estado de ejecución.
- **Transiciones Críticas**: 
  - Actualización de `TransicionEstado` con etiquetas dinámicas y requerimiento obligatorio de observaciones.
  - Aplicación de severidad visual (`error`/`warning`) en botones de acción crítica.
- **Gestión de Observadores**:
  - Parámetro `incluirInactivos` en el servicio de observadores para permitir la visualización de personal fuera de servicio sin afectar la lógica de disponibilidad del sistema.
  - Refactorización de filtros de integridad para distinguir entre "disponibilidad operativa" y "existencia administrativa".

### Frontend
- **Priorización de UX**: Reordenamiento de acciones sugeridas situando "Editar Etapas" como primera opción en el panel operativo.
- **Administración de Observadores**: 
  - Actualización de `ObservadoresView` para mostrar la nómina completa (activos e inactivos), permitiendo la reactivación y edición histórica.
  - Refactorización del composable `useObservadores` para soportar la carga condicional de inactivos.

### Base de Datos
- **Migración Segura**: Implementación de la migración `add_reversal_transitions` para incorporar las nuevas reglas del motor de estados sin afectar la integridad de los datos existentes.

---

## 2026-03-11 - Optimización de Backups y Segregación de Esquemas

### Base de Datos
- **Segregación de Esquemas**: Implementación de arquitectura multi-esquema para optimizar el rendimiento y la organización de datos:
  - `public`: Lógica de negocio core (Mareas, Buques, Observadores).
  - `audit`: Repositorio centralizado de auditoría y logs.
  - `datos_api`: Almacenamiento de alto volumen para trayectorias y reportes externos (zarpadas/arribos).
- **Inmutabilidad de Schemas**: Protección de esquemas históricos preservando datos durante la migración técnica.
- **Enum de Jobs**: Incorporación de `DAILY_BACKUP` al sistema de colas.

### Backend
- **Motor de Backup Multi-Esquema**:
  - Generación de archivos `.sql` independientes por esquema con compresión final en un único `.zip`.
  - Protección de archivos mediante contraseñas dinámicas.
  - **Metadatos JSON**: Generación de manifiestos con firma HMAC (integridad), fecha, comentario y esquemas incluidos.
- **Restauración Selectiva**: Implementada lógica para restaurar individualmente uno o varios esquemas desde un único archivo de respaldo.
- **Backup Automático Diario**:
  - Sistema de configuración dinámica vía `system_status`.
  - Gestión integrada con `JobQueue` para trazabilidad y reintentos.
  - **Corrección de Duplicidad**: Lógica robusta anti-duplicados que unifica el uso de zona horaria local (Argentina) en nombres de archivos y verificaciones del scheduler.
- **Hotfix de Despliegue**: Resolución definitiva de conflictos de tipos (`@types/pg`) en Docker mediante `pnpm overrides` en el paquete local del backend.

### Frontend
- **Vista de Backup Premium**:
  - Rediseño completo de `BackupView` con layout de tres columnas y acentos visuales dinámicos.
  - Sección de "Programación Automática" con toggle y selector de hora interactivo.
  - Implementación de selección multivariante de esquemas para respaldos manuales.
- **Componente TimePicker**: Nuevo componente personalizado para selección de hora horaria, optimizado para usabilidad y diseño corporativo.
- **Soporte SVG Inline**: Reemplazo de iconos de librería por SVGs para asegurar visibilidad en builds de producción runtime-only.

---

## 2026-03-10 - Mejora UX: Navegación de Años Históricos

### Frontend
- **Indicador Visual de Año Histórico**: 
  - **AdminLayout**: Se implementó un fondo de advertencia (color ámbar tenue con textura SVG) cuando el parámetro `selectedYear` es menor al año actual, facilitando la identificación inmediata de que se están consultando datos antiguos.
- **Centro de Comando**:
  - **DashboardView**: Ocultada la vista operativa y KPIs en años históricos, sustituyéndolos por un elegante *Empty State* que explica que el pulso en tiempo real solo aplica al año en curso, incluyendo además un acceso directo para restablecer la vista al año actual.
- **Vigilancia de Flota**:
  - **MonitorSeguimientoView**: Restricción de visualización general del mapa en años pasados con su correspondiente placeholder. 
  - La visualización del mapa se mantiene plenamente operativa para la inspección individual de trayectorias (ej: haciendo clic desde una marea específica).
  - Ocultación lógica de barras laterales (`VesselListSidebar` y `MonitorSidebar`) para evitar confusión.
- **Unificación Estética**:
  - **AppSidebar & PlanificacionSidebar**: Se sincronizaron ambos selectores de "Año Operativo" usando componentes nativos `<select>` con estilos premium, asegurando que toda la aplicación use el mismo rango (2024 en adelante) y estética.
  - **VesselListSidebar**: El selector de "Pesquerías" fue rediseñado con la misma estética premium del sistema.

## 2026-03-07 - Sincronización PNA Resiliente e Ingestión Opcional

### Backend
- **PnaApiService**: Implementada fragmentación automática de rangos de fecha mayores a 20 días (configurable vía `PNA_API_SYNC_SAFE_RANGE_DAYS`), delegando el procesamiento a tareas en segundo plano.
- **Modo Ingesta Pura**: Soporte para el flag `onlyIngest` que permite la carga masiva de datos históricos omitiendo la generación de alertas y validaciones pesadas, optimizando el rendimiento.
- **Arquitectura de Jobs**: Actualización de los procesadores `PnaApiSyncProcessor` y `PnaTrackingSyncProcessor` para propagar el flag `onlyIngest`.
- **Independencia de Sincro**: La ejecución manual ahora opera independientemente de si la sincronización automática está activada o desactivada.

### Frontend
- **PnaSyncDashboardView**:
  - Incorporación de switch "Realizar validaciones y alertas" para controlar el modo de ingesta.
  - Implementación de **diálogo de confirmación estandarizado** (`ConfirmationDialog`) para advertir sobre procesos de rango extendido y fragmentación.
  - Mejora en el feedback de usuario diferenciando procesos síncronos de encolados.
- **Alerts Admin Service**: Actualización de la interfaz de servicios para soportar parámetros extendidos de sincronización manual.

## 2026-03-02 - Consistencia Visual y Flexibilidad de Interfaz

### Frontend
- **SearchInput**: Flexibilización del componente eliminando el ancho fijo por defecto (`sm:w-64`), permitiendo que se adapte al contenedor padre mediante `w-full`.
- **Refinamiento de Anchos**: Se estandarizó el uso de `w-96` para los buscadores en las vistas principales (`BandejaView`, `PanelOperativoView`, `FlujoMareasView`) para mejorar la legibilidad en pantallas grandes.
- **Sidebar de Buques**: Se logró la uniformidad de anchos entre el buscador y el dropdown de pesquerías.
- **Auditoría**: Optimización de clases en la vista de logs aprovechando la flexibilidad del componente.

## 2026-02-28 - Ampliación de Inmutabilidad de Datos de Marea

### Base de Datos
- **Integridad de Marea**: Creación de una nueva migración segura (`CREATE OR REPLACE FUNCTION`) para extender la inmutabilidad a los campos: `buqueId`, `observadorPrincipalId`, `pesqueriaId` y `artePrincipalId`.
- **Flexibilidad de Estados**: Se incluyó el estado `A_REASIGNAR` como estado preparatorio, permitiendo la edición de todos los campos críticos al igual que en `DESIGNADA`.

### Backend
- **Mareas Service**: Actualización de la validación en el método `update` para sincronizar las reglas de negocio con los triggers de la base de datos.
- **Suite de Pruebas**: Actualización de tests unitarios en `MareasService` para validar las nuevas restricciones y el soporte para el estado `A_REASIGNAR`. Total de tests pasando: 196 (incluyendo integridad global).

### Frontend
- **EditMareaDesignadaDialog**: Actualización de la lógica de edición para soportar el estado `A_REASIGNAR` y bloquear selectivamente campos protegidos (Buque, Observador, Pesquería y Arte) cuando la marea está en ejecución.
- **MareaDetalleView & EditarMareaView**: Implementación de restricciones visuales (`disabled`) en los campos críticos de la pestaña general, garantizando que el usuario no pueda intentar ediciones inválidas fuera de los estados permitidos.

## 2026-02-27 - Restricción de Edición y Mejoras Operativas

## 2026-02-27 - Mejoras en Monitor Satelital y Visualización Operativa

### Backend
- **Tracking Service**: Enriquecimiento de la respuesta de tracking con datos de pesquería y tipo de flota mediante inyección de relaciones en Prisma. Correcta restauración de la clave `include` para estabilidad de consultas.
- **Mareas Service**: Inclusión de la relación `tipoFlota` en el dashboard operativo para permitir la visualización de la categoría del buque.

### Frontend
- **Monitor de Seguimiento (Nautical Map)**:
  - **Filtro por Pesquería**: Implementación de un dropdown dinámico que filtra simultáneamente la lista de buques y los marcadores en el mapa.
  - **Auto-Zoom Inteligente**: El mapa realiza un ajuste de encuadre (*fit bounds*) automático al cambiar de pesquería para mostrar únicamente los buques filtrados.
  - **Búsqueda Texto-Mapa Independiente**: Se ha desacoplado el buscador de texto para que filtre la lista de buques sin ocultar los marcadores en el mapa, facilitando la localización visual dentro de una pesquería.
  - **Diseño Refinado**: Reubicación de la pesquería y flota debajo del nombre del buque con jerarquía visual mejorada y respeto a la capitalización original de los datos.
- **Versión Móvil**:
  - **MobileMonitorControls**: Adaptación completa de los filtros de pesquería y visualización avanzada para dispositivos móviles.
  - **UX Optimizada**: Implementación de auto-cierre del cajón de flota al seleccionar una pesquería para agilizar la interacción táctil.
- **Vistas Operativas (Panel y Flujo de Mareas)**:
  - **Nueva Columna "Pesquería / Flota"**: Adición de información crítica directamente en las tablas principales de gestión.
  - **Ordenamiento y Multilínea**: La nueva columna es totalmente ordenable y utiliza un diseño de dos líneas para maximizar la densidad de información.
  - **Consistencia Móvil**: Integración de los nuevos campos en las tarjetas de la versión móvil de mareas.
  - **Distribución de Mareas (Gantt)**: Se actualizó el gráfico para ordenar y agrupar visualmente las mareas por "Pesquería" y "Flota". Además, el diseño del eje Y se reestructuró en dos líneas alineadas a la izquierda, destacando el nombre del buque sobre su contexto operativo.


## 2026-02-26 - Matriz de Experiencia de Observadores

### Backend
- **Matriz Tridimensional**: Refactorización del modelo `ExperienciaObservador` para soportar la relación Observador-Pesquería-Tipo de Flota.
- **Campo Experiencia**: Adición del campo `experiencia` (Int) para registrar el total de mareas acumuladas en cada contexto.
- **Lógica de Persistencia**: Implementación de Upsert/Delete masivo basado en la tupla de 3 IDs. Se eliminan registros automáticamente si ambos campos (`valor` y `experiencia`) son nulos.
- **Optimización de Payload**: Incremento del límite de aceptación de JSON a **10MB** en el servidor de NestJS para soportar el guardado de matrices extensas.
- **Migración de Producción**: Creación de la migración SQL segura `20260226182000_add_experiencia_observador_tridimensional` utilizando `gen_random_uuid()` para máxima compatibilidad.

### Frontend
- **Interfaz Transpuesta**: Rediseño completo con Observadores en filas y Pesquerías/Flotas en columnas para una mejor interpretación de la capacidad operativa.
- **Doble Entrada por Celda**: Implementación de inputs para Calificación (estrellas) y Mareas (experiencia numérica) en cada intersección.
- **Sticky Headers Avanzados**: Encabezados fijos en ambos ejes (filas de observadores y dos niveles de columnas) con soporte para scroll horizontal y vertical suave.
- **Optimización de Envío**: Refactorización de `saveChanges` para enviar únicamente celdas que contienen datos, reduciendo drásticamente el tamaño del payload y mejorando la eficiencia de red.
- **Filtrado Restringido**: Configuración de visualización limitada a flotas tipo "Altura" (`ALTURA_FRESQUERO` y `ALTURA_CONGELADOR`) según requerimiento operativo.

#### [NEW] Filtrado por Pesquería y Ocultación Dinámica
- **Checkboxes de Pesquería**: Implementación de filtros dedicados para pesquerías, permitiendo segmentar la matriz según los recursos de interés.
- **Filtrado Inteligente (Modo Vista)**: Ocultación automática de columnas de flota (Fresquero/Congelador) y filas de observadores que no contienen datos para los filtros seleccionados, despejando drásticamente la interfaz.
- **Diseño Responsivo**: Refinamiento de la barra de filtros y la vista móvil para asegurar una experiencia táctil fluida y legible en pantallas pequeñas.
- **Acciones Rápidas**: Botones para selección/deselección masiva de pesquerías.

#### [NEW] Tarjeta de Matriz de Experiencia en Dashboard de Planificación
- **Acceso Rápido**: Nueva tarjeta en `PlanificacionHomeView` con navegación directa a la Matriz de Experiencia, consistente con el diseño del resto del dashboard.

### Backend
#### [FIX] StatsService: Fallo de Dependencia en Tests
- **Resolución de Inyección**: Corrección de fallo en `StatsService.spec.ts` agregando el mock necesario de `PlanificacionService`, requerido para las funcionalidades de exportación de cobertura recientemente integradas.

## 2026-02-25 - Implementación Módulo de Planificación y Requerimientos de Cobertura


### General
#### [NEW] Incorporación del Rol "Planificador"
- **Acceso Directo**: Se creó un nuevo sistema de permisos para el rol `planificador`, permitiendo el acceso exclusivo al módulo de planificación.
- **Identidad Visual**: El rol ya es seleccionable en la gestión de usuarios y habilita el enlace "Planificación" en el menú lateral principal.

### Backend
#### [NEW] API de Gestión de Requerimientos
- **PlanificacionController/Service**: Implementación de endpoints para la gestión de la matriz de requerimientos (`GET` / `POST` / `PUT`).
- **Upsert Masivo**: Lógica avanzada para guardar o actualizar múltiples registros de requerimientos en una sola operación atómica.
- **Soporte Prisma**: Integración del modelo `RequerimientoCobertura` en el esquema y sincronización con PostgreSQL.

#### [FIX] Sincronización de Base de Datos y Estabilidad
- **Resolución de Migración (P2021)**: Aplicación robusta de esquemas mediante DDL manual y sincronización del historial de Prisma (`migrate resolve`) para entornos multi-schema complejos.
- **Pruebas de Integración**: Suite de tests unitarios completa para servicios y controladores, asegurando el 100% de éxito en la lógica de negocio.

### Frontend
#### [NEW] Matriz de Requerimientos de Cobertura
- **Interfaz Reactiva**: Tabla tipo matriz Pesquería vs. Meses con soporte para edición en tiempo real.
- **Totales Inteligentes**: Cálculo automático de totales mensuales (verticales) y resúmenes anuales.
- **Modos de Trabajo**: Implementación de switch "Lectura / Edición" para prevenir cambios accidentales.

#### [NEW] Diseño de Dashboard de Planificación
- **Centro de Control**: Nuevo dashboard estético basado en tarjetas premium, alineado con el diseño del panel de administración.
- **Navegación Intuitiva**: Uso consistente del ícono de buque (`ShipIcon`) para identificar las herramientas de planificación.

#### [MOD] Estética Institucional y Botones Premium
- **Paleta de Colores**: Adopción de la paleta cromática de **ApexCharts** y mapeo semántico por pesquería (ej. Calamar: Azul, Merluza: Verde) para una identificación visual inmediata.
- **Botón de Guardado**: Rediseño del botón de acción principal con iconografía `CheckIcon`, efectos de elevación (`shadow-primary/20`) y micro-interacciones (`active:scale-95`).
- **Feedback Dinámico**: Mejora en el estado de carga del botón con un preloader circular sincronizado con el proceso de persistencia.

#### [MOD] Simplificación de Interfaz y Densidad de Datos
- **Eliminación de Switch Redundante**: Se eliminó el control "Ocultar vacías"; el sistema ahora automatiza este comportamiento en modo visualización para reducir el ruido cognitivo.
- **Ocultamiento de Flotas**: Filtrado inteligente de filas individuales de flota (Fresquero/Congelador) cuando no poseen asignación en el año operativo.
- **Restricción de Categorías**: Lógica de catálogo que limita la planificación exclusivamente a flotas de altura (`ALTURA_FRESQUERO` y `ALTURA_CONGELADOR`).

#### [FIX] Tipado y Estándares
- **CatalogosService**: Enriquecimiento de la interfaz `TipoFlota` con el campo `codigo` para permitir el filtrado dinámico robusto.
- **Consistencia Visual**: Unificación de iconografía y estilos de botones en todos los sidebars del proyecto.
- **Pruebas de Cobertura**: Ajuste de la suite de tests unitarios de `RequerimientosCoberturaView` para validar la lógica de ocultamiento dinámico de filas vacías.

#### [MOD] Refinamiento de Estadísticas y Cobertura
- **Nomenclatura Semántica**: Reemplazo global del término "Ejecutado" por **"Cubierto"** en el módulo de cobertura de buques para una mayor precisión institucional.
- **Interactividad de Leyenda**: Restauración del toggle nativo de ApexCharts para ocultar/mostrar series, optimizando el orden de renderización de las barras `[Requerido, Cubierto]`.
- **Layout de Gráficos**: Reubicación estratégica de la leyenda al panel derecho (`position: 'right'`) logrando un diseño columnar que maximiza el área útil del canvas.
- **Exportación de Datos**: Habilitación del menú `toolbar` en el gráfico de Cobertura, permitiendo la descarga en formatos **SVG**, **PNG** y **CSV** para análisis externo.
- **Tooltip Analítico Homogéneo**: Unificación del componente tooltip entre la vista agregada y la desglosada, asegurando desgloses de flota y métricas de esfuerzo consistentes en todos los modos.
- **Corrección de Espaciado**: Ajuste de márgenes internos en tooltips para prevenir la superposición visual de etiquetas y valores numéricos.

## 2026-02-24 - Badge de Intención de Cierre en Panel Operativo
### Added
- **Backend (Mareas)**: Nuevo estado `A_REASIGNAR` para clasificar mareas que no pudieron ser ejecutadas y deben ser reasignadas.
- **Frontend (Mareas)**: Implementado `EditMareaDesignadaDialog` para edición simplificada de mareas en estado `DESIGNADA`.
- **Frontend (Mareas)**: Visualización y opciones de filtrado del estado `A_REASIGNAR` en `PanelOperativoView`.
- **Frontend (Mareas)**: Autocompletado automático de Pesquería y Arte de pesca principal al seleccionar o cambiar el buque en la nueva dialog de edición.
- **Frontend (Mareas)**: Visualización de comentarios de movimientos en la actividad reciente del panel de detalle.
- **Frontend (Mareas)**: Implementada visibilidad condicional en el panel de detalle para ocultar secciones operativas (Avance y Logística) en estados `DESIGNADA` y `A_REASIGNAR`.
- Soporte para persistencia automática de fuentes de arribo en cierres sugeridos por intención de usuario.

### Backend
#### [FEAT] MareasService: Cálculo de `intencion_cierre` en Dashboard
- **Nueva propiedad en respuesta**: El endpoint `GET /mareas/operativo` ahora incluye el campo `intencion_cierre` (booleano) en cada ítem, calculado evaluando el campo `metadata.opcionesCierre.finalizarMareaAlArribo` de la última etapa de la marea.
- **Consulta Prisma optimizada**: La consulta del dashboard fue refactorizada para seleccionar explícitamente todos los campos necesarios, incluido el campo `metadata` de cada etapa.
- **Ordenamiento explícito de etapas**: Las etapas se ordenan por `nroEtapa` en el código antes de evaluar la última etapa activa.
- **Tipado estricto**: El acceso a la metadata se realiza usando la interfaz `MareaEtapaMetadata` en lugar de casts `as any`.

#### [FIX] MareasController/MareasService: Propagación del Usuario Autenticado
- **Corrección real de error de compilación**: El método `update` del controller ahora inyecta el usuario autenticado (`@GetUser() user: User`) y lo propaga al servicio, en lugar de intentar obtener `usuarioId` del DTO.
- **Auditoría correcta**: El log de cambio de intención de cierre ahora registra correctamente el `usuarioId` del usuario que realizó el guardado.

### Frontend
#### [FEAT] PanelOperativoView: Badge de Intención de Cierre
- **Indicador visual**: Se muestra un badge rojo "Finalizando" con ícono de bandera junto al estado de cada marea en `EN_EJECUCION` que tenga la intención de cierre activada.
- **Vista escritorio y móvil**: El badge aparece en ambas vistas (tabla y tarjetas).

## 2026-02-24 - Filtro de Pesquería en Vistas Operativas

### Frontend
#### [FEAT] Filtro de Pesquería Dinámico
- **Panel Operativo y Flujo**: Implementación de un nuevo dropdown de selección de pesquería que permite filtrar las mareas activas de forma precisa.
- **Lógica Dinámica**: El listado de pesquerías disponibles se extrae automáticamente de las mareas presentes en la vista, asegurando que solo se muestren opciones con resultados.
- **Diseño Premium**: Integración estética del selector con icono de buque y estilos de diseño modernos, manteniendo la coherencia visual con la barra de búsqueda corporativa.
- **Filtrado Combinado**: El nuevo filtro opera en conjunto con la búsqueda por texto y los estados de marea, permitiendo una segmentación avanzada.

## 2026-02-23 - Vista Rápida Operativa en Fichas del Dashboard

### Backend
#### [MOD] MareasService: Identificadores en Movimientos Recientes
- **UUID de Marea**: Se añadió el campo `mareaId` al retorno de `getRecentMovements`, permitiendo que el frontend vincule eventos puntuales con la gestión completa de la marea.

### Frontend
#### [FEAT] Interactividad en Fichas del Dashboard
- **Movimientos de Buques**: Implementación de filas clicables con cursor interactivo que activan el modal de vista rápida (`MareaQuickDetailModal`).
- **Mareas Próximas a Finalizar**: Integración de vista rápida en las tarjetas de arribos inminentes, permitiendo inspeccionar el desglose de etapas y días navegados sin salir del panel general.
- **Tipado**: Actualización de interfaces de movimientos para incluir la referencia al UUID de marea.

## 2026-02-21 - Gestión de Roles de Acceso y Permisos de Catálogo

### Backend
#### [NEW] Incorpación de Roles Restringidos
- **Nuevos Roles**: Implementación de los roles `desarrollador` y `visitante` en el enum `ValidRoles`.
- **Estrategia de Solo Lectura**: Ambos roles operan bajo una política estricta de solo lectura, sin acceso a operaciones de escritura (`POST`, `PATCH`, `DELETE`) en los módulos administrativos.
- **Enriquecimiento del Seed**: Adición de usuarios de prueba para `desarrollador` y `visitante` con estado `isActive: false` para activación manual.
- **Tipado de Semilla**: Actualización de la interfaz `SeedUser` para soportar la propiedad opcional `isActive`.

#### [MOD] Reasignación de Permisos de Gestión (Buques y Observadores)
- **Ajuste de Autorización**: Se eliminó el permiso de creación y edición al rol `coordinador` para los catálogos de Buques y Observadores.
- **Transferencia de Privilegios**: El rol `tecnico_datos` ahora cuenta con permisos de edición para estos catálogos, junto con el rol `admin`.

### Frontend
#### [MOD] AppSidebar: Acceso Analítico para Desarrolladores
- **Visibilidad Contextual**: Se habilitó el acceso a la vista de **Estadísticas** exclusivamente para el rol `desarrollador`.
- **Exclusión Administrativa**: Los roles `desarrollador` y `visitante` permanecen excluidos de las secciones de "Sistema" y "Administración" en el menú lateral.

#### [MOD] Router y Constantes: Integración de Nuevos Roles
- **Autorización de Rutas**: Actualización de `router/index.ts` para autorizar explícitamente el acceso de `desarrollador` a la ruta de estadísticas.
- **Etiquetado de Interfaz**: Incorporación de etiquetas para los nuevos roles en la constante `ROLE_LABELS`.

#### [MOD] Vistas de Catálogo: Lógica de Edición Reactiva
- **Protección de UI**: Actualización de las propiedades computadas `canEdit` en `BuquesView` y `ObservadoresView` para reflejar la nueva política de permisos (`admin` + `tecnico_datos`).

## 2026-02-20 - Integridad de Fuentes en Gestión de Etapas de Marea

### Backend
#### [FIX] MareasService: Protección de Metadatos de Fuentes
- **Persistencia Robusta**: Se modificaron los métodos `syncStages` y `update` para prevenir que campos de fuentes (`fuentesZarpada`, `fuentesArribo`) sean sobreescritos con valores nulos ante payloads incompletos o manuales.
- **Tipado de DTOs**: Actualización de `MareaEtapaDto` para incluir formalmente los campos de fuentes, garantizando coherencia de tipos y evitando errores de compilación.
- **Tests Unitarios**: Implementación de casos de prueba robustos en `mareas.service.spec.ts` validando la protección de fuentes. Total de tests pasando: 180 (backend global).

### Frontend
#### [MOD] GestionEtapasMareaDialog: Preservación de Seguimiento
- **Clonación Profunda**: Mejora en la lógica de inicialización del diálogo para asegurar que los metadatos de fuentes se conserven al editar etapas existentes.
- **Payload Íntegro**: Garantía de re-transmisión de los objetos de fuentes al backend durante la confirmación de acciones, cerrando el ciclo de persistencia de datos automatizados (PNA/Tracking).

## 2026-02-20 - Persistencia Histórica y Trazabilidad de PNA API

### Backend
#### [NEW] Tabla `pna_zarpadas_arribos`: Histórico Estructurado de Movimientos
- **Mapeo Integral**: Implementación de una nueva tabla que persiste todos los reportes recibidos de la API de PNA (mapeo 1 a 1 de los campos del XML).
- **Deduplicación Robusta**: Uso de `external_combo_id` (Puerto + Buque + Fecha + Estado) como clave única para evitar duplicados entre sincronizaciones.
- **Auditoría de Procesamiento**: Inyección de `fromDate` y `toDate` en el payload de las tareas `PNA_API_SYNC`, permitiendo auditar el rango exacto de cada ejecución (automática o manual).
- **Refactorización de Scheduler**: Mejora en `SchedulerService` para calcular rangos dinámicos basados en la última sincronización exitosa.
- **Nomenclatura Consistente**: Renombramiento de la tabla histórica a `pna_zarpadas_arribos` para alinearse con los estándares del proyecto.

### Seguridad y Estándares
#### [NEW] Protección contra Migraciones Destructivas
- **Reglas del Agente**: Implementación de una nueva política de seguridad en `.agent/rules/database.md` que prohíbe el uso de comandos destructivos (`--force-reset`) sin autorización explícita, previniendo pérdidas accidentales de datos en desarrollo y producción.
- **Migraciones no Destructivas**: Establecimiento del uso de `migrate resolve` y DDL manual aditivo para cambios de esquema complejos sobre bases de datos con datos críticos.

### Frontend
#### [FIX] AlertTrafficLight: Estabilidad de Renderizado
- **Corrección de Lints**: Eliminación de saltos de línea inválidos en el template que provocaban errores de compilación tras la integración de rangos de trayectoria.

---

## 2026-02-18 - Flexibilización de Disponibilidad de Observadores

### Backend
#### [MOD] MareasService & ObservadoresService: Disponibilidad de Observadores
- **Coexistencia de Estados**: Se flexibilizó la validación para permitir que un observador pueda ser designado para una nueva marea mientras aún se encuentra navegando (`EN_EJECUCION`). Esta lógica ahora es consistente con los triggers de integridad de la base de datos.
- **Restricción de Designación**: El sistema ahora solo bloquea la asignación si el observador ya posee otra marea en estado `DESIGNADA` (reserva futura), permitiendo una mejor planificación operativa.

---

## 2026-02-17 - Lógica de Metadatos Limpios y Precisión de Trayectorias

### Backend
#### [MOD] AlertAutomationService: Persistencia de Metadatos de Etapa
- **Persistencia de `eventDate`**: Se modificaron los payloads de confirmación automática para inyectar explícitamente el campo `eventDate` dentro de los metadatos de etapa (`fuentesZarpada` y `fuentesArribo`). Esto garantiza que la hora precisa detectada sea accesible perpetuamente sin recalculaciones.
- **Interfaces Estables**: Creación de `StageMetadata.interface.ts` para estandarizar la estructura de metadatos persistidos en las etapas de marea.

### Frontend
#### [NEW] TrajectoryRangeUtils: Motor de Resolución de Tiempos
- **Centralización de Fechas**: Nueva utilidad que resuelve automáticamente la fecha de referencia del evento priorizando `eventDate` y manejando normalización de objetos complejos (Prisma/DateTime).
- **Cálculo de Ventana Inteligente**: Implementación de buffers de +/- 12 horas (expandible a 36h para fechas truncadas) para garantizar la visualización completa de la trayectoria en el mapa.
- **Suite de Pruebas**: Implementación de 14 tests unitarios validando normalización, detección de hora y resolución de incongruencias.

#### [MOD] Integración Global de Trayectorias (Frontend)
- **Dashboard y Listados**: Integración de la nueva lógica de resolución en `RecentMovements`, `AlertTrafficLight`, `InboxAlertCard` y `AlertsLogView`, eliminando parches locales de cálculo de fechas.
- **Gestión Avanzada**: Refactorización de `AlertManagementDialog` y `TrajectorySourceBadge` para utilizar el motor centralizado de rangos.
- **Correcciones de Compilación**: Refactorización de `AlertTrafficLight.vue` para extraer lógica de formateo a funciones helper, resolviendo errores de compilación causados por saltos de línea en el template.

---

## 2026-02-15 - Restauración de Gratícula y Estabilidad de Mapas

### Frontend
#### [MOD] NauticalMap: Restauración de Gratícula (Retícula)
- **Etiquetas de Coordenadas**: Se implementaron etiquetas dinámicas de latitud y longitud en los cuatro bordes (N, S, E, W) del área visible del mapa, mejorando drásticamente la orientación espacial.
- **Optimización Visual**: El diseño final utiliza una fuente compacta de `7px` con opacidad `0.7` y sombreado de contraste, asegurando legibilidad sin interferir con la visualización de buques o trayectorias.
- **Actualización Reactiva**: La gratícula se recalcula y reposiciona automáticamente en tiempo real durante el desplazo (`moveend`) y zoom (`zoomend`) del mapa.
- **Legibilidad Multi-Tema**: Ajuste de colores dinámicos basados en variables CSS para una visualización óptima tanto en modo claro como oscuro.

#### [FIX] Estabilidad de Capas Argenmap
- **Diagnóstico de Conectividad**: Se verificó el funcionamiento de las capas `Argenmap` (IGN), confirmando que las intermitencias reportadas se debieron a caídas del servicio externo. Se mantuvo la configuración de URLs compatibles con los estándares actuales del IGN.

---

## 2026-02-15 - Mejoras en Control de Auditoría

### Backend
#### [MOD] Mareas: Restricción de Gestión y Acciones
- **Backend: MareasController**: Se restringieron los endpoints de creación, actualización y ejecución de acciones de flujo (`claim`, `create`, `update`, `executeAction`) exclusivamente para roles `admin` y `tecnico_datos`.
- **Frontend: MareaContextDetailContent**: Se ocultó la sección "Acciones sugeridas" y el botón de edición de detalles para usuarios sin permisos técnicos (incluyendo coordinadores), manteniendo solo el acceso a visualización y trayectoria.

#### [MOD] Alerts Management: Restricción de Roles
- **Backend: AlertsController**: Se restringieron los endpoints de creación (`POST`) y actualización (`PATCH`) de alertas exclusivamente para los roles `admin` y `tecnico_datos`.
- **Frontend: InboxAlertCard**: Se implementó una validación de roles para ocultar el botón "Gestionar" a usuarios sin permisos técnicos (incluyendo el rol de coordinador), manteniendo únicamente la visualización de detalles en alertas históricas.

#### [MOD] UsersController: Apertura de Lectura de Usuarios
- **Acceso Global de Lectura**: Se eliminó la restricción de roles administrativos para el endpoint `GET /api/users`, permitiendo que cualquier usuario autenticado acceda a la lista para poblar filtros y componentes del frontend.
- **Seguridad de Escritura**: Se mantienen las protecciones estrictas de rol (`admin`, `coordinador`) para todas las operaciones de creación, modificación y eliminación.

#### [MOD] AuditService: Búsqueda Omnipresente por Usuario
- **Búsqueda por Nombre Completo**: Se habilitó el filtrado por `fullName` del usuario en todos los tipos de logs de auditoría (API, Entidades, Eventos y Navegación).
- **Consistencia de Filtros**: Se implementó el parámetro `busqueda` en los métodos de consulta de Entidades y Eventos que previamente lo ignoraban.
- **Estabilidad de Pruebas**: Refactorización de `DateUtils` para usar `DateTime.now()` de Luxon en lugar del objeto `Date` nativo, garantizando la consistencia del mocking temporal en las suites de tests.

### Calidad y Estándares
#### [NEW] Skill: Estándares de Testing Profesional
- **Metodología Unificada**: Creación del skill `testing-standards` que define una estrategia robusta para el diseño de pruebas unitarias e integración en NestJS y Vue.
- **Triple Cobertura**: Establecimiento de la obligación de probar casos de éxito, error y borde en cada nueva funcionalidad.
- **Protección de Reglas de Negocio**: Directiva estricta sobre la prioridad de la lógica de negocio durante la resolución de fallos en los tests.

### Frontend
#### [MOD] AuditLogsView: Contexto Temporal y Búsqueda Reactiva
- **Formato 24h Compacto**: Actualizada la lista de logs para mostrar fecha y hora en formato `DD/MM HH:MM` (militar) para optimizar el espacio en la barra lateral.
- **Agrupamiento por Fecha**: Implementación de separadores visuales discretos que agrupan los eventos por día, facilitando el análisis cronológico.
- **Scroll al Tope Automático**: El contenedor de la lista ahora vuelve automáticamente al inicio al cambiar de página, mejorando la usabilidad en la navegación del historial.
- **Integración de SearchInput**: Reemplazado el buscador nativo por el componente corporativo `SearchInput.vue`.
- **Búsqueda Reactiva (Watch)**: Implementada actualización automática de resultados mientras se escribe (debounce implícito vía API), mejorando la agilidad de inspección.

---

## 2026-02-14 - Precisión Operativa y Coherencia de Datos en Estadísticas

### Backend
#### [FIX] StatsService: Atribución de Días por Pesquería
- **Filtrado por Etapa**: Refactorizada la función `getDashboardStatsDetail` para que, al filtrar por una pesquería, solo se contabilicen los días de marea correspondientes a las etapas de ese recurso. Esto elimina el sobreconteo en mareas con objetivos múltiples.
- **Sincronización de Filtros Globales**: Implementada la propagación de filtros de protocolización (`protocolizationStartDate`/`EndDate`) de forma independiente al rango de actividad, garantizando que los totales del dashboard y del detalle coincidan exactamente.
- **Unificación UTC Total**: Eliminados los últimos residuos de zona horaria local en los cálculos de periodos mensuales.

### Frontend
#### [FIX] Comportamiento de Leyenda y Tooltips
- **Salvaguarda de Leyenda**: Implementada detección profunda en `ChartWidget.vue` (basada en DOM `closest('.apexcharts-legend')`) para evitar que al hacer clic en un nombre de serie para ocultarla se abra accidentalmente el diálogo de detalle.
- **Intersección de Fechas**: Los clics en meses del gráfico de cobertura y tendencia ahora calculan automáticamente la intersección con el filtro global de fechas, mostrando solo el detalle del periodo visible.
- **Estabilidad de Eventos**: Reforzados todos los manejadores de eventos en `EstadisticasView.vue` para asegurar una respuesta coherente independientemente del tipo de gráfico (Donut, Scatter, Bar).

---

## 2026-02-14 - Mareas: Persistencia, Auditoría y Diagnóstico de Correo

### Backend
#### [MOD] MareasService: Sincronización Avanzada de Etapas
- **Borrado Total de Etapas**: Corregido el método `update` para permitir el borrado de todas las etapas de una marea cuando el listado se envía vacío pero definido.
- **Suite de Pruebas CRUD**: Implementación de `mareas-crud.spec.ts` que valida el ciclo de vida completo de la marea, asegurando la integridad referencial y la sincronización recursiva de etapas.

#### [MOD] MailService: Trazabilidad y Auditoría de Errores
- **Registro de Fallos**: Integración con `AuditService` para registrar automáticamente cualquier error de envío de email (SMTP/Conexión) como un evento `ERROR_ENVIO_EMAIL`.
- **Refactorización de AuthService**: El servicio de autenticación ahora utiliza el wrapper centralizado `MailService`, garantizando que las fallas en recuperación de contraseña sean visibles en los logs de auditoría.
- **Corrección de Puerto SMTP**: Forzada la conversión de `SMTP_PORT` a tipo `Number` en el `MailModule`, resolviendo fallos intermitentes de negociación SSL con Gmail provocados por el tipado débil de las variables de entorno.

#### [NEW] Utilidad de Diagnóstico SMTP
- **Producción Ready**: El script `test-smtp.ts` se ha reubicado en `src/mail/scripts/` para ser compilado automáticamente.
- **Comandos Universales**: Agregado `test:smtp:prod` para ejecutar el diagnóstico en entornos de producción usando únicamente `node`.
- **Carga de Entorno Robusta**: Implementada detección dinámica del archivo `.env` basada en el directorio de trabajo (`process.cwd()`).

#### [REF] AuthService: Enlaces de Recuperación Resilientes
- **Refactorización de Enlaces de Recuperación**: Se ha centralizado y robustecido la generación de enlaces para "Olvidé mi contraseña" en el backend, asegurando compatibilidad con diferentes configuraciones de `FRONTEND_URL`.
- **Mejora en Exportación Excel**: Se ajustaron las columnas del reporte de "Cobertura Mensual" para coincidir con la vista de detalle (ocultando columnas irrelevantes para el mes) y se corrigió la codificación de caracteres especiales en los encabezados.
- **Configuración Simplificada**: La variable `FRONTEND_URL` ahora debe contener solo la URL base (ej. `https://mi-app.com`), eliminando la necesidad de incluir `/reset-password` en la configuración.

### Frontend
#### [FIX] Persistencia de Pesquería por Etapa
- **MareaDetalleView**: Corregido el mapeo de datos al guardar una marea. Se eliminó la sobreescritura accidental del `pesqueriaId` de la etapa con el de la marea, permitiendo que cada tramo conserve su recurso asignado correctamente.

### Calidad y Estabilización
- **Pruebas de Estadísticas**: Corregidas las expectativas de esfuerzo en `stats.service.spec.ts` para reflejar con precisión la métrica de "días-buque" versus días únicos.
- **Auditoría de Mocks**: Estabilización de los mocks de Prisma en las suites de prueba para garantizar resultados determinísticos en la validación de estados globales.
- **Hito de Verificación**: Se alcanzó la marca de **189 tests pasando exitosamente** en el backend.

---

## 2026-02-14 - Métricas de Esfuerzo y Consistencia en Estadísticas

### Backend
#### [MOD] StatsService: Desglose Mensual y de Flota con Esfuerzo
- **Cálculo de Días por Mes**: Se ha optimizado `getUniqueVesselsCount` para calcular y devolver los días de marea navegados mensualmente y desglosados por tipo de flota.
- **Nuevo Campo `diasPeriodo`**: Implementado en `getDashboardStatsDetail` para garantizar el cálculo exacto de días dentro de cualquier rango solicitado, asegurando consistencia con los gráficos.
- **Lógica de Flota por Pesquería**: Se refactorizó `getFleetDistributionByFishery` para clasificar los buques según la pesquería de su **etapa actual o más reciente**, garantizando una visualización fiel de la flota activa.
- **Integridad de Tipos**: Actualización de interfaces y servicios para manejar las nuevas métricas de esfuerzo en todo el flujo de datos.

### Frontend
#### [FEAT] Gráfico de Cobertura Mensual Combo (Barras + Línea)
- **Visualización Dual**: Transformación del gráfico de cobertura en un combo chart que muestra Buques Únicos (barras) y Días de Marea (línea) mediante ejes Y independientes.
- **Tooltip Premium**: Rediseño del tooltip para mostrar ambas métricas simultáneamente con desgloses por flota y estilos visuales mejorados.
- **Interactividad Dinámica**: Los clics en el gráfico ahora abren un detalle contextualizado al mes seleccionado.

#### [FEAT] Nuevos Gráficos de Análisis por Pesquería
- **Mareas vs Días por Pesquería**: Implementación de gráfico de barras y línea para correlacionar la frecuencia de salidas con el esfuerzo real (días) por cada recurso.
- **Matriz de Perfil Operativo (Scatter Plot)**: Nueva visualización de "Esfuerzo vs Frecuencia" que permite identificar clústeres operativos por pesquería, facilitando el análisis de intensidad de pesca.

#### [MOD] Detalle de Estadísticas Contextualizado
- **Títulos Inteligentes**: La columna de días en la tabla de detalle ahora muestra dinámicamente el mes analizado (ej: "Días Mayo") cuando se accede desde el gráfico de cobertura.
- **Fila de Totales**: Implementación de una fila final (`tfoot`) en la tabla de detalle que totaliza automáticamente el esfuerzo (días en periodo y totales), con estilos premium.
- **Simplificación de Interfaz**: Ocultamiento inteligente de columnas secundarias ("Total Marea") en vistas mensuales para maximizar la legibilidad y asegurar que el total coincida con el gráfico.

### Prueba y Calidad
- **Validación de Métricas**: Actualización de la suite de pruebas unitarias del backend para contemplar la distribución exacta de días y buques por mes y flota.
- **Distribución de Flota**: Nueva suite de pruebas `mareas-distribution.spec.ts` para validar la prioridad de etapas en la clasificación de pesquerías.
- **Aseguramiento de Tipos**: Refactorización de interfaces en frontend y backend para mantener la coherencia del modelo de datos de estadísticas.

---

## 2026-02-12 - Mejoras en Visualización de Trayectorias y Fuentes de Monitoreo

### Backend
#### [MOD] MareasService: Enriquecimiento de Eventos de Movimiento
- Se añadió el campo `vesselId` al retorno de la función `getRecentMovements`.
- **Propósito**: Permitir que el frontend obtenga el identificador del buque necesario para realizar consultas de trayectoria directamente desde los eventos del dashboard.

### Frontend
#### [NEW] TrajectorySourceBadge: Componente de Fuente Unificado e Interactivo
- **Generalización de Badge**: Evolución del anterior `TrackingSourceBadge` para soportar múltiples fuentes de datos.
- **Soporte PNA**: Integración de la fuente PNA con estilos visuales específicos (color `warning`).
- **Interactividad**: Ambos badges (Tracking y PNA) ahora permiten abrir el mapa de trayectoria al hacer clic.
- **Modo Abreviado**: Nueva prop `abbreviated` para visualización compacta (`TRK` / `PNA`) en tablas con alta densidad de información como la de movimientos de buques.
- **Rango Temporal Automático**: Implementación de lógica contextual que añade +/- 12 horas a eventos puntuales (zarpadas/arribos), permitiendo visualizar 24 horas de navegación centradas en el evento.

#### [MOD] Dashboard: Integración de Trayectorias
- **Movimientos Recientes**: Los eventos de movimiento ahora incluyen badges interactivos abreviados.
- **Zarpadas y Arribos**: El semáforo de tráfico ahora utiliza el nuevo componente permitiendo visualización inmediata desde el mapa.

#### [MOD] Central de Alertas & Mareas
- **InboxAlertCard**: Actualización para permitir la apertura del mapa desde las fuentes de la alerta.
- **AlertsLogView**: Generalización de la visualización de fuentes en el histórico de alertas.

### General
- **Patrones de UI**: Mejora en la consistencia estética de los badges (fuente de 9px, negrita, padding compacto) para igualar el estilo de badges oficiales (ej: PNA).

---

### Backend

#### [NEW] Integración con API Externa de Buques
- **Cliente Fishery**: Implementado sistema de adaptadores para consumir datos oficiales de buques desde la API del Ministerio de Pesca.
  - Arquitectura basada en patrón Strategy con `FisheryApiAdapter` (producción) y `FisheryMockAdapter` (desarrollo/testing).
  - Configuración mediante variables de entorno: `FISHERY_API_ENDPOINT`, `FISHERY_API_TIMEOUT`, `USE_MOCK_FISHERY_API`.
- **Sincronización de Datos**: Nuevo servicio `VesselSyncService` que actualiza automáticamente la información de buques.
  - Sincronización inteligente basada en umbral configurable (`VESSEL_SYNC_THRESHOLD_DAYS`, default: 7 días).
  - Enriquecimiento de datos: matrícula, bandera, año de construcción, MMSI, tipo de buque, señal distintiva, dimensiones (eslora, manga, puntal), arqueo, calado, puerto de asiento, dotación mínima, y estado de registro.
- **Migración de Base de Datos**: Añadidos 15 nuevos campos a la tabla `buques` para almacenar datos oficiales.
  - Migración aditiva segura aplicada sin pérdida de datos existentes.
  - Nuevo campo `fecha_ultima_actualizacion_api` para tracking de sincronizaciones.

#### [NEW] Sistema Genérico de Tareas Programadas (Job Queue)
- **Infraestructura de Tareas**: Implementado sistema escalable de procesamiento asíncrono de trabajos en segundo plano.
  - Nueva tabla `job_queue` con soporte para múltiples tipos de tareas, reintentos automáticos, priorización y backoff exponencial.
  - `SchedulerService` con bloqueo distribuido usando `system_status` para prevenir ejecución concurrente en entornos multi-instancia.
  - Configuración mediante cron expressions (`JOB_SCHEDULER_INTERVAL_CRON`).
- **Evolución de Base de Datos**: Actualización del modelo `JobQueue` para soportar analíticas avanzadas.
  - Implementación de tipos enumerados nativos (`JobStatus`, `JobType`).
  - Nuevos campos de diagnóstico: `duration` (ms), `result` (JSON), `stack_trace` y `error_message`.
  - Índices optimizados para consultas de estadísticas y series temporales.
- **Backend Analítico y API de Administración**:
  - `JobQueueStatsService`: Servicio dedicado para el cálculo eficiente de KPIs, rendimiento por tipo de tarea y distribución de errores.
  - `JobQueueController`: Endpoints de gestión y monitoreo protegidos mediante el decorador `@Auth(ValidRoles.admin)`.
  - Funcionalidades: Listado paginado con filtros, detalle técnico de ejecución, reintento manual de tareas fallidas y cancelación.
- **Procesadores de Tareas**: Arquitectura extensible basada en la interfaz `JobProcessor`.
  - `VesselSyncProcessor`: Procesa sincronizaciones de buques con rate limiting configurable (`VESSEL_SYNC_RATE_LIMIT_MS`).
  - Preparado para futuras tareas de sincronización de trayectorias y procesos batch.

#### [FIX] Corrección de Búsqueda en API de Buques
- **Problema**: El sistema intentaba buscar buques por matrícula (no soportado por API PNA)
- **Solución**: Implementado orden de búsqueda correcto
  - **1º Búsqueda por ID MBPC** (más preciso)
  - **2º Fallback por MMSI** (nuevo método implementado)
  - **3º Fallback por nombre** (última opción)
- **Mejoras en payload del job**:
  - Añadido `nombreBuque`, `idMbpc` y `mmsi` al payload
  - Logs mejorados con información detallada de búsqueda
- **Impacto**: Las sincronizaciones ahora funcionan correctamente con la API real de PNA

#### [FEAT] Mejoras en Dashboard de Gestión de Tareas
- **Ordenamiento por Columnas**: Implementada funcionalidad de ordenamiento clickeable en el datatable de tareas.
  - Columnas ordenables: Tipo, Estado, Intentos, Última Ejecución, Duración
  - Indicador visual con icono chevron que rota según dirección (asc/desc)
  - Orden por defecto: Fecha de creación (más reciente primero)
- **Filtro por Tipo de Tarea**: Agregado selector dropdown en el gráfico "Evolución de Tareas".
  - Permite filtrar datos por tipo específico o ver todos los tipos
  - Filtrado local sin llamadas adicionales al backend
  - Reagrupación automática de datos al cambiar filtro
- **Corrección de Visualización**: Ajustadas configuraciones de ApexCharts para evitar overflow de etiquetas del eje X.
  - Aplicado en gráficos de "Evolución de Tareas" y "Rendimiento de Tareas"
  - Configuraciones: `chart.offsetY: -10`, `chart.parentHeightOffset: 0`, `xaxis.labels.offsetY: -5`

#### [FEAT] Discriminación de Flota por Pesquería
- **Análisis Multidimensional**: El gráfico de pesquerías ahora integra la discriminación por tipo de flota (Fresquero/Congelador).
- **Subsegmentos Inteligentes (Stacked Bars)**: Implementación de barras de progreso apiladas en la lista de distribución para visualizar la proporción de cada flota manteniendo el total de la pesquería.
- **Visualización Condicional**: La información de flota solo se muestra en pesquerías de composición mixta, manteniendo una interfaz limpia para casos simples.
- **Tooltip Premium**: Rediseño del tooltip del gráfico de dona con desgloses automáticos por flota y totales consolidados.
- **Optimización de UX en Detalles**: Eliminación de restricciones de altura y scroll en la lista expandida de buques para una visualización fluida de la flota completa.

#### [FIX] Corrección de Campos Nulos en Job Queue
- **Problema**: Los campos `result`, `worker_id`, `duration` y `stack_trace` siempre quedaban en `null` al procesar tareas.
- **Solución**: Actualizado `SchedulerService` para capturar y guardar correctamente estos campos:
  - **`duration`**: Calculado midiendo tiempo de inicio y fin de ejecución (en milisegundos)
  - **`workerId`**: Generado automáticamente basado en `hostname-PID` (ej: `DESKTOP-ABC-12345`)
  - **`result`**: Captura del valor retornado por el procesador de tareas
  - **`stackTrace`**: Guardado completo de `error.stack` cuando hay errores (no solo el mensaje)
- **Mejora en Procesadores**: `VesselSyncProcessor` ahora retorna información útil sobre la sincronización ejecutada (vesselId, vesselName, mmsi, syncedAt).
- **Impacto**: Mejor diagnóstico de tareas, métricas precisas de rendimiento, y debugging facilitado con stack traces completos.

#### [FEAT] Reingeniería y Unificación de Alertas de Marea
- **Centralización de Inteligencia**: Nueva arquitectura basada en `EventCorrelationService` que unifica las reglas de negocio entre Tracking CSV y API PNA.
- **Source Stacking Robusto**: Implementada deduplicación inteligente que permite que múltiples fuentes (satelital y oficial) validen una misma alerta sin duplicarla, acumulando historial de evidencia.
- **Integridad Ferroviaria de Puertos**: Refactorización del motor de correlación para priorizar estrictamente los IDs del catálogo de puertos sobre los nombres comerciales, eliminando falsos positivos en puertos con nombres idénticos pero ubicaciones distintas.
- **Alertas Especializadas**: 
  - **Discreparancia de Puerto**: Generada cuando hay una diferencia de ubicación basada en IDs técnicos.
  - **Incongruencia de Fecha**: Detectada cuando la jornada local del evento no coincide con la registrada.
  - **Recomendación de Fin de Marea**: Sugerencia proactiva para cerrar mareas antiguas si se detecta un arribo y existe una nueva marea designada.

#### [QUALITY] Pruebas Unitarias y Estabilización Técnica
- **Suite de PNA API Service**: Implementada nueva suite integral `pna-api.service.spec.ts` con 16 casos de prueba cubriendo deduplicación, auditoría y reglas de negocio.
- **Estabilización de Matching V2**: Actualizados tests de `tracking.matching_v2.spec.ts` para cumplir con los nuevos estándares de integridad de puertos por ID.
- **Cobertura Global**: Se alcanzó un hito de **148 tests pasando con 100% de éxito**, garantizando la estabilidad total del núcleo de gestión de mareas tras la unificación de lógica.
- **Arquitectura de Mocks**: Refactorización del `beforeEach` en tests críticos para asegurar aislamiento total y evitar persistencia de estados entre ejecuciones.

---

## 2026-02-08 - Sincronización Incremental y Estabilidad

### ✨ Nuevas Funcionalidades
#### [NEW] Sincronización Incremental de PNA
- **Persistencia de Estado**: Implementado uso de la tabla `system_status` con la clave `LAST_PNA_SYNC` para optimizar peticiones recurrentes.
- **Lógica de Ventana Deslizante**:
  - Cálculo automático de rango: desde las **00:00:00** de la última fecha exitosa hasta las **23:59:59** del día actual.
  - El solapamiento garantiza la captura de movimientos reportados con demora por la API externa.
- **Atomicidad de Proceso**: La fecha de sincronización solo se actualiza en la base de datos si el procesamiento de movimientos finaliza exitosamente (`COMPLETED`).

### 🛠️ Mejoras y Refactorizaciones
#### [MOD] Refinamiento de Correlación de Eventos
- **Prioridad de Auditoría**: Reordenamiento de reglas en `EventCorrelationService` para priorizar la detección de `DISCREPANCY_DATE` (días distintos) sobre la clasificación de eventos antiguos.
- **Flexibilidad de Puerto**: Mejora en la robustez de la lógica de puerto, permitiendo procesar eventos sin IDs técnicos (ej: detecciones automáticas) sin generar falsas discrepancias.
- **Secuencialidad Estricta**: Restaurada la validación que requiere una etapa abierta para registrar alertas de arribo, alineando el sistema con el flujo operativo real.

### 🧪 Testing
- **Depuración Integral de Regresiones**: Resolución de 2 fallos críticos en la suite de `EventCorrelation` y `Tracking` derivados de la unificación de lógica.
- **Validación de Sincronización**: Nuevos tests para la gestión de `LAST_PNA_SYNC` y el cálculo de rangos UTC/Local.
- **Hito de Calidad**: Verificación exitosa de los **148 tests del sistema** en una sola ejecución limpia.
---

## 2026-02-09 - Filtros Temporales y Precisión de Estadísticas

### ✨ Nuevas Funcionalidades
#### [FEAT] Control Avanzado de Tiempo en Estadísticas
- **Nueva Barra de Filtros Temporal**: Implementado componente `TimeFilterBar` con diseño premium para la vista de estadísticas.
  - Soporte para selección rápida de **Mes** y **Trimestre**.
  - Modo **Rango Manual** utilizando dos calendarios (Desde/Hasta) para mayor flexibilidad.
  - Función de **Reset** para volver rápidamente a la vista anual por defecto.
- **Micro-segmentación Dinámica**: Los gráficos y resúmenes se actualizan en tiempo real al cambiar los límites temporales.
- **Análisis Detallado por Pesquería**: Implementado gráfico de barras con doble eje Y (Mareas vs Días Navegados) para correlacionar esfuerzo y frecuencia por recurso.
- **Gráfico de Perfil Operativo (Scatter Plot)**: Introducción de una matriz de Esfuerzo vs Frecuencia con leyendas de color por pesquería, permitiendo identificar clústeres operativos (intensivos, rotativos, eventuales).
- **Rediseño de Layout y UX**: 
  - Optimización del espacio mediante una distribución en grilla para visualización simultánea de gráficos.
  - **Modal de Detalles Inteligente**: Implementado diseño responsivo que fuerza la vista de tarjetas en dispositivos móviles para una mejor legibilidad, manteniendo la opción de tabla únicamente en escritorio.
- **Interactividad Optimizada**: Corregida la navegación desde el gráfico de Perfil Operativo (Scatter Plot) para que el filtrado por pesquería sea preciso al hacer clic en las burbujas.
- **Contexto Operativo en Dashboard**: Añadido badge de número de etapa (E2, E3, etc.) en el listado de personal navegando para identificar mareas con múltiples tramos.

#### [FEAT] Sincronización de Pesquería y Flota
- **Tooltips Enriquecidos**: El gráfico de "Participación por Pesquería" ahora incluye el desglose de buques por tipo de flota, replicando la inteligencia del Dashboard.
- **Visibilidad Integral de Flota**: Adición de la columna y campo "Flota" en el modal de detalles de estadísticas (vistas de tabla y tarjeta).
- **Consistencia Estética Unificada**: Unificación del grosor (55%) de los gráficos de donut en toda la aplicación para una experiencia visual coherente y premium.

### 🛠️ Mejoras y Refactorizaciones
#### [MOD] Reingeniería del Motor de Cálculo de Días
- **Intersección Precisa de Periodos**: Refactorización de `DateUtils.calculateUniqueDays` para soportar rangos arbitrarios, permitiendo cálculos exactos de días navegados dentro de un mes o trimestre específico.
- **Sincronización con "Fecha Real"**: Implementada limitación física de actividad basada en la fecha actual (`Now`) para evitar que mareas en ejecución proyecten días hacia el futuro.
- **Localización de Gráficos**: Implementada fusión profunda (deep merge) en `ChartWidget` para asegurar que todas las visualizaciones y menús de descarga estén en español, independientemente de la configuración individual.
- **Extensión de API de Estadísticas**: El backend ahora provee el desglose de tipos de flota dentro de los agregados de pesquería.

### 🐛 Correcciones (Bug Fixes)
#### [FIX] Integridad de Datos y Consultas
- **Proyecciones Futuras**: Corregido bug donde meses sin actividad mostraban datos de mareas en curso. Ahora el backend valida periodos futuros y devuelve resultados vacíos preventivamente.
- **Validación de Tipos en Prisma**: Resuelto error de sintaxis UUID (`invalid input syntax for type uuid: "no-match"`) mediante el uso de filtros lógicamente imposibles pero tipados correctamente (`anioMarea: -1`).
- **Exportación Filtrada**: Asegurado que la descarga de Excel respete estrictamente los filtros de tiempo aplicados en pantalla.
---

## 2026-02-10 - Estandarización de Interfaz y Mejora de Automatización

### ✨ Nuevas Funcionalidades
#### [FEAT] Estandarización Estética SIGMA Admin
- **Unificación de Diseño**: Aplicación de los estándares estéticos del módulo `JobQueue` a todo el panel de Gestión de Alertas.
  - Refactorización de `AlertsLogView.vue` con redondeos `rounded-xl` y bordes simplificados.
  - Limpieza tipográfica eliminando pesos `font-black` excesivos.
  - Estandarización de `ConfirmationDialog.vue` como componente base de confirmación para el sistema.
- **Navegación Administrativa**: Incorporación del acceso directo "Gestión de Alertas" al `AdminSidebar.vue`.

### 🛠️ Mejoras y Refactorizaciones
#### [MOD] Evolución de Lógica de Autoconfirmación
- **Soporte para Múltiples Etapas**: El sistema ahora es capaz de autoconfirmar alertas de `ZARPADA` para mareas que ya están `EN_EJECUCION`, creando automáticamente una nueva etapa si la anterior ha finalizado.
- **Humanización de Auditoría**: Corrección del tipo de evento en `AlertAutomationService` de `RESOLUCION_AUTOMATICA` a `AUTO_RESOLUCION` para asegurar títulos legibles y bien formados en el historial.
- **Detalle de Procesamiento Batch**: Implementación de vista de resultados detallada y colapsable en el diálogo de procesamiento masivo.
- **Estándares UI**: Unificación del lenguaje visual de los diálogos con `JobQueueView` (redondeos `rounded-xl` y sombras consistentes).

### 🧪 Testing y Calidad
- **Fortalecimiento del Servicio de Tracking**: Implementación de una nueva suite integral de integridad ([tracking.integrity.spec.ts](file:///app/backend/src/mareas/tracking.integrity.spec.ts)) con cobertura de casos de borde críticos:
    - Procesamiento robusto de archivos CSV vacíos, corruptos o con formatos numéricos inválidos.
    - Resolución de buques mejorada mediante búsqueda insensible a mayúsculas y espacios.
    - Garantía de idempotencia a nivel de puntos de trayectoria mediante `skipDuplicates`.
    - Manejo seguro de buques no registrados sin interrupción del proceso batch.
- **Hito de Estabilidad Operativa**: Logrado el paso exitoso de los **163 tests globales** del monorepo, asegurando la integridad total del sistema de gestión de mareas tras las mejoras de robustez.

### 🐛 Correcciones (Bug Fixes)
- **Integridad de Base de Datos**: Añadida migración manual `20260210162532_add_fuentes_marea_etapa` para corregir la ausencia de columnas `fuentes_zarpada` y `fuentes_arribo` en entornos de producción/staging.
- **Robustez de Automatización**: Corregido error `Invalid time value` en procesamiento de alertas mediante la inclusión de `fechaDetectada` obligatoria en el flujo.
#### [MOD] Refinamiento de Resolución de Buques
- **Priorización Optimizada**: Reordenamiento de los criterios de búsqueda en `TrackingService` para priorizar identificadores internacionales y robustos.
  - Nuevo orden: **1º MMSI**, **2º Nombre**, **3º Matrícula Nacional**, **4º Matrícula SIOP**.
- **Lógica de Matrícula Flexible**: Implementación de búsqueda numérica que ignora ceros a la izquierda y caracteres alfanuméricos no confiables en la base de datos.
- **Auto-corrección Segura**: Sincronización automática de identificadores oficiales (`mmsi`, `matriculaSiop`) al detectarse por cualquier criterio. El campo `matricula` original de la base de datos se mantiene inalterado por seguridad.
- **Importación Silenciosa**: Los buques no encontrados en la base de datos durante la importación CSV ahora se omiten silenciosamente sin reportar error, permitiendo procesar archivos con listas parciales de buques registrados.
- **Robustez de Pruebas**: Estabilización de la suite de integridad y actualización de mocks para contemplar el nuevo flujo de resolución competitiva.
#### [NEW] Sincronización de Tracking PNA (XML/SOAP)
- **Motor de Tracking Oficial**: Implementado nuevo servicio `PnaTrackingService` para la ingesta de posiciones históricas desde la API de Prefectura.
  - Sincronización fragmentada en bloques de 3 horas para manejar grandes volúmenes de datos sin saturar la memoria.
  - Soporte para entornos de desarrollo mediante `USE_MOCK_FISHERY_API` (carga de XML local).
- **Infraestructura de Jobs Asíncronos**:
  - Nuevo `JobType.PNA_TRACKING_SYNC` registrado en la base de datos (Migración Segura).
  - `PnaTrackingSyncProcessor`: Procesador dedicado con soporte para rate limiting configurable (`TRACKING_SYNC_RATE_LIMIT_MS`) para llamadas secuenciales seguras.
  - Programación automática cada 2 horas mediante cron en `SchedulerService`.
- **Integración y UX**:
  - Control manual en el frontend (`AlertsLogView.vue`) mediante el botón "Sincronizar Tracking" (Verde SIGMA).
  - Feedback visual inmediato: indicación del número de tareas fragmentadas encoladas.
  - **Cliente SOAP Oficial (WSPescaExternos)**:
  - Implementación completa del cliente SOAP 1.2 para el consumo del servicio `GetPosicionesHistoricas` (sscpwsexternos.prefecturanaval.gob.ar).
  - Configuración flexible mediante variables de entorno y soporte continuo para `USE_MOCK_FISHERY_API`.
  - Manejo robusto de errores de red y respuestas SOAP.
- **Estandarización y Precisión**:
  - **Corrección de Zona Horaria**: Forzado el uso explícito de **UTC** y validación ISO/Format en el parseo de fechas del XML, eliminando desfases locales.
  - **Unificación de Estatus**: El mapa ahora refleja la "fecha de última actualización" sincronizada tanto desde CSV como desde la API de PNA.
  - **Mejoras UX en Gestión**: Corregidos rótulos del gráfico de rendimiento de tareas para mostrar nombres legibles ("Sincro Buques", "Sincro Tracking") y agregados filtros específicos en la cola de trabajos.
  - **Estandarización de Bloqueos**: Unificación de la clave de bloqueo del planificador a `JOB_SCHEDULER_LOCK` para consistencia y seguridad en entornos multi-instancia.
