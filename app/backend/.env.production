# --- Configuración de Base de Datos ---
DB_PASSWORD=BTEt7LnI9GOoqbmRa1Y5Mt8b
DB_NAME=mareas_db
DB_HOST=db
DB_PORT=5432
DB_USERNAME=postgres
# IMPORTANTE: En producción usar el host 'db' (nombre del servicio en docker-compose)
DATABASE_URL="postgresql://${DB_USERNAME}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}"

# --- Configuración de la Aplicación ---
PORT=3000
API_DOMAIN=mareas-api.innovamdp.com
HOST_API=https://${API_DOMAIN}/api
FRONTEND_URL=https://tusistema.com
JWT_SECRET=y7d7HDbefXc5DcehEZpEPdmWX3xw3X8SSHm4D9E3om3JVfPk
BACKUP_SECRET=8grccccVkKkfcJ8G3HZV9kHzKDqlCKUG0AVgKP8tp81
BACKUP_ZIP_PASSWORD=DSk8bazVZVeXdcqE
APP_TIMEZONE=America/Argentina/Buenos_Aires

# --- Configuración de Mail (Producción) ---
# SMTP_HOST=
# SMTP_PORT=587
# SMTP_USER=
# SMTP_PASS=
# SMTP_SECURE=false
# SMTP_FROM="Sigma <no-reply@tusistema.com>"

# --- Configuración de Mail (Pruebas locales) ---
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_USER=
SMTP_PASS=
SMTP_SECURE=false
SMTP_FROM="no-reply@miapp.local"

# --- Rutas de Archivos ---
BACKUP_PATH="./backups"
STATIC_PATH="./static"
UPLOADS_PATH="./uploads"
