# --- Configuración de Base de Datos ---
DB_PASSWORD=oGlPQWZ4l7md4hpOEiwC13NodF4maJQYElQZeaW45KN
DB_NAME=mareas_db_staging
DB_HOST=db-staging
DB_PORT=5432
DB_USERNAME=postgres
# IMPORTANTE: En staging usar el host 'db-staging' (nombre del servicio en docker-compose de staging)
DATABASE_URL="postgresql://${DB_USERNAME}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}"

# --- Configuración de la Aplicación ---
PORT=3000
API_DOMAIN=mareas-api-staging.innovamdp.com
STAGING_API_DOMAIN=mareas-api-staging.innovamdp.com
HOST_API=https://${API_DOMAIN}/api
FRONTEND_URL=https://mareas-obs-desarrollo.netlify.app
JWT_SECRET=kBxdyDysGwbaXuLpyyGFVBg9sAifCArXBpqUor1T45y
BACKUP_SECRET=8grccccVkKkfcJ8G3HZV9kHzKDqlCKUG0AVgKP8tp81
BACKUP_ZIP_PASSWORD=DSk8bazVZVeXdcqE
APP_TIMEZONE=America/Argentina/Buenos_Aires
ALERT_CHECK_INTERVAL_MINUTES=15

# --- Configuración de Mail (Producción) ---
# SMTP_HOST=
# SMTP_PORT=587
# SMTP_USER=
# SMTP_PASS=
# SMTP_SECURE=false
# SMTP_FROM="Sigma <no-reply@tusistema.com>"

# --- Configuración de Mail (Pruebas locales) ---
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_USER=
SMTP_PASS=
SMTP_SECURE=false
SMTP_FROM="no-reply@miapp.local"

# --- Rutas de Archivos ---
BACKUP_PATH="./backups"
STATIC_PATH="./static"
UPLOADS_PATH="./uploads"

# SISTEMA DE AUDITORÍA
# ============================================

# Nivel de auditoría: ALL (todo) o CRITICAL (solo operaciones críticas)
AUDIT_LEVEL=ALL
AUDIT_API_ENABLED=false
AUDIT_NAVIGATION_ENABLED=true
AUDIT_ENTITIES_ENABLED=true
AUDIT_EVENTS_ENABLED=true
AUDIT_ASYNC_ENABLED=true


# ============================================
# REDIS (para colas asíncronas)
# ============================================
REDIS_HOST=redis-staging
REDIS_PORT=6379
