PORT=3000
HOST_API=http://localhost:3000/api
FRONTEND_URL=http://localhost:5173/reset-password
STAGING_API_DOMAIN=api-staging.miapp.local

# DATABASE (conexión interna entre contenedores)
DB_PASSWORD=MyStagingPassWord@123
DB_NAME=MareasDB_Staging
DB_HOST=db-staging
DB_PORT=5432
DB_USERNAME=postgres

DATABASE_URL="postgresql://postgres:MyStagingPassWord%40123@db-staging:5432/MareasDB_Staging"

BACKUP_PATH="./backups"
STATIC_PATH="./static"
UPLOADS_PATH="./uploads"

JWT_SECRET=StagingSecretKey777!
BACKUP_SECRET=StagingBackupSecretXYZ123
BACKUP_ZIP_PASSWORD=StagingZipPass
APP_TIMEZONE=America/Argentina/Buenos_Aires
ALERT_CHECK_INTERVAL_MINUTES=15

# REDIS (conexión interna entre contenedores)
REDIS_HOST=redis-staging
REDIS_PORT=6379

# SMTP (conexión interna entre contenedores)
SMTP_HOST=mailhog-staging
SMTP_PORT=1025
SMTP_USER=
SMTP_PASS=
SMTP_SECURE=false
SMTP_FROM="staging@inidep.local"

# AUDITORÍA
AUDIT_LEVEL=ALL
AUDIT_API_ENABLED=true
AUDIT_NAVIGATION_ENABLED=true
AUDIT_ENTITIES_ENABLED=true
AUDIT_EVENTS_ENABLED=true
AUDIT_ASYNC_ENABLED=true
