PORT=3000
HOST_API=http://localhost:3000/api
FRONTEND_URL=http://localhost:5173/reset-password

DB_PASSWORD=MySecr3tPassWord@as2
DB_NAME=MareasDB
DB_HOST=localhost
DB_PORT=5435
DB_USERNAME=postgres

DATABASE_URL="postgresql://postgres:MySecr3tPassWord%40as2@localhost:5435/MareasDB?schema=public"
BACKUP_PATH="./backups"

JWT_SECRET=Est3EsMISE3Dsecreto32s
BACKUP_SECRET=
BACKUP_ZIP_PASSWORD=
APP_TIMEZONE=America/Argentina/Buenos_Aires
ALERT_CHECK_INTERVAL_MINUTES=15

SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_USER=
SMTP_PASS=
SMTP_SECURE=false
SMTP_FROM="no-reply@miapp.local"


## Gmail real
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=465 
# SMTP_USER=daniel.dt.inidep@gmail.com
# SMTP_PASS=iwekhutimxltgpxu
# SMTP_SECURE=true
# SMTP_FROM="daniel.dt.inidep@gmail.com"

# ============================================
# SISTEMA DE AUDITORÍA
# ============================================

# Nivel de auditoría: ALL (todo) o CRITICAL (solo operaciones críticas)
AUDIT_LEVEL=ALL

# Auditoría de API
AUDIT_API_ENABLED=true
AUDIT_API_INCLUDE_REQUEST_BODY=true
AUDIT_API_INCLUDE_RESPONSE_BODY=true
AUDIT_API_MAX_BODY_SIZE=10000
AUDIT_API_RETENTION_DAYS=90

# Auditoría de Navegación (Frontend)
AUDIT_NAVIGATION_ENABLED=true
AUDIT_NAVIGATION_RETENTION_DAYS=30

# Auditoría de Entidades (Triggers DB)
AUDIT_ENTITIES_ENABLED=true

# Auditoría de Eventos de Negocio
AUDIT_EVENTS_ENABLED=true

# Procesamiento Asíncrono (requiere Redis)
AUDIT_ASYNC_ENABLED=true

# ============================================
# REDIS (para colas asíncronas)
# ============================================
REDIS_HOST=localhost
REDIS_PORT=6379
